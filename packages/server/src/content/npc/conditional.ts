import type { NpcTrigger } from "@takeoff/shared";

/** Conditional NPC triggers — active across multiple rounds, filtered by rounds field if set. */
export const CONDITIONAL_NPC_TRIGGERS: NpcTrigger[] = [];
